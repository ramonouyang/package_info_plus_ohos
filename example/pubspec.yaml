name: package_info_plus_ohos_example
description: Demonstrates how to use the package_info_plus_ohos plugin.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.4.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  package_info_plus: ^6.0.0
  package_info_plus_ohos:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
